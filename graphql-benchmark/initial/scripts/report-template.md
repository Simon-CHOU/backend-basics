# 协议对比实验报告

## 实验环境

| 项目 | 值 |
|------|-----|
| CPU | (请填写) |
| 内存 | (请填写) |
| OS | (请填写) |
| Java | 21 |
| Spring Boot | 3.3.5 |
| JMeter | 5.6.3 |
| 数据量 | 1000 订单, 200 用户 |

## 实验设计

### 被测接口
三种协议实现同一个 BFF 功能：根据 `orderId` 返回订单详情 + 关联用户信息。

### 负载模型
- 并发线程：100
- 预热时间：5s
- 压测时长：60s
- 数据集：10 个不同 orderId 循环使用

### 控制变量
- 同一 JVM、同一机器
- 同一 Application Service (`OrderDetailUseCase`)
- 同一 Mock 仓储（无 DB 噪声）
- 同一日志级别 (WARN)
- 同一 JVM 参数 (-Xms256m -Xmx256m)

## 实验结果

### P99 延迟 (ms)

| 协议 | P50 | P90 | P95 | P99 | P99.9 | 吞吐量 (req/s) |
|------|-----|-----|-----|-----|-------|-----------------|
| REST + JSON | | | | | | |
| GraphQL | | | | | | |
| gRPC + protobuf | | | | | | |

### 内存占用

| 协议 | 堆使用量 (平均) | 堆使用量 (峰值) | GC 次数 | GC 总停顿 (ms) |
|------|-----------------|-----------------|---------|----------------|
| REST + JSON | | | | |
| GraphQL | | | | |
| gRPC + protobuf | | | | |

### 序列化 CPU 开销

| 协议 | 序列化热点方法 | CPU 采样占比 |
|------|---------------|-------------|
| REST + JSON | Jackson ObjectMapper | |
| GraphQL | DGS QueryExecutor | |
| gRPC + protobuf | Protobuf parseFrom/writeTo | |

## 分析与结论

### REST + JSON
- **优势**：
- **劣势**：
- **适用场景**：

### GraphQL
- **优势**：
- **劣势**：
- **适用场景**：

### gRPC + protobuf
- **优势**：
- **劣势**：
- **适用场景**：

## 局限性

1. Mock 仓储消除了远程 I/O 噪声，真实场景下网络延迟会稀释协议差异。
2. 单接口测试，未覆盖 GraphQL 的字段裁剪和批量查询优势。
3. Payload 规模固定，不同数据量级下结论可能不同。
4. 未测试流式场景（gRPC streaming）。

## 结论（实验驱动）

> 在本实验负载下，在当前实现方式与运行环境下：
> - 某协议在某指标上占优
> - 某协议以某代价换取某能力
>
> 没有"天生最佳"的协议，只有适合特定场景的选择。
